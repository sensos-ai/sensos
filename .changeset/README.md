# Changesets

Release Changesets are generated centrally by the manual Prepare release workflow. Contributors do not need to add Changesets to ordinary pull requests. The CLI, shared, and client packages form one fixed version group; the CLI remains private and is distributed only as a binary.
